import Card from "../../UI/Card";

const Picogram = (props) => {
  return (
    <Card>
      <div>{/* 이미지 */}</div>
      <div>
        <h3>Picogram(피코그램)</h3>
        {/* 
                        설명/소개
                        주요기능 프론트/백
                        개발기간
                        gitHub
                        웹사이트
                        Deployment(배포)
                    */}
      </div>
    </Card>
  );
};

export default Picogram;
